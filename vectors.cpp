//Given an array of N integers, right rotate the array by one place.
#include<bits/stdc++.h>
using namespace std;
int main(){
    int n = 5;
    vector<int> v(5,2);
    for(int i = 0; i < n; i++){
        cout << v[i] << " ";
    }

    return 0;
}